# Copy this file to frontend/android/keystore.properties and fill in your real values.
# Keep the real keystore.properties file out of version control.

storeFile=app/release-keystore.jks
storePassword=replace-me
keyAlias=replace-me
keyPassword=replace-me
